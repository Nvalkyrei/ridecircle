apiVersion: v1
kind: ConfigMap
metadata:
  name: ridecircle-config
  namespace: ridecircle
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # Database configuration will be handled via secrets
  # Redis configuration will be handled via secrets
